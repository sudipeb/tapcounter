import 'package:flutter/material.dart';

class TapView extends StatelessWidget {
  const TapView({super.key});
  @override
  Widget build(context) {
    return Scaffold(appBar: AppBar(title: Text('Faster Finger')));
  }
}
